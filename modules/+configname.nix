# This should get set as part of the edit script, but if this is not pointed to where
# Your config actually is on your system ( relative to home/settings.username ) rebuildb and rebuilds won't work
# you can still manually run a rebuild.
{flake.settings.configname = "nixos-kickstart";}
